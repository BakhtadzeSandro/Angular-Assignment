/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NamesPipe } from './names.pipe';

describe('Pipe: Namese', () => {
  it('create an instance', () => {
    let pipe = new NamesPipe();
    expect(pipe).toBeTruthy();
  });
});
